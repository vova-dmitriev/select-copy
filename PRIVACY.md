# Privacy

SelectCopy runs locally. It reads the current focused element only after a user selection gesture, writes selected plain text to the standard macOS pasteboard, and shows an optional local toast.

SelectCopy does not send text over the network, maintain a copy history, or collect analytics. Accessibility permission is required because macOS isolates text selections between applications.
